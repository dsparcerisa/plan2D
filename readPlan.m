function plan = readPlan(fileName)
% struct plan = readPlan(string fileName)
% Reads a plan into a structure from specified fileName

% Needs to fill in the following data
plan.fileName = 
plan.name =
plan.energy = 
plan.Z = 
end

